Fixed long-running :class:`~airflow.providers.common.sql.operators.generic_transfer.GenericTransfer`
tasks being incorrectly killed by the scheduler due to missing heartbeats during
the paginated data transfer loop. The operator now emits a heartbeat after each
page of data is transferred, preventing false zombie detection on tasks running
longer than ``task_instance_heartbeat_timeout``.
