# Agent Skills Evaluation Scenarios

Each scenario is a standalone "exam" that verifies the agent skills
work correctly for a real contributor workflow.

| Scenario | Coverage | Status |
|----------|----------|--------|
| scenario_01_fix_and_test.md | host context, static checks, uv test run; surfaces `context: either` via the `run-tests-in-breeze` skill in Step 1's skill listing | ready |
| scenario_02_breeze_context.md | covers `context: breeze` — not yet written | not yet written |

Run a scenario by following its steps manually or by giving it to
an AI agent as its task description.
