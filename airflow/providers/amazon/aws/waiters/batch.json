{
  "version": 2,
  "waiters": {
    "JobComplete": {
      "delay": 300,
      "operation": "DescribeJobs",
      "maxAttempts": 100,
      "description": "Wait until job is SUCCEEDED",
      "acceptors": [
        {
          "argument": "jobs[].status",
          "expected": "SUCCEEDED",
          "matcher": "pathAll",
          "state": "success"
        }
      ]
    }
  }
}
