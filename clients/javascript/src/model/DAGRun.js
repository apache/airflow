/**
 * Airflow API (Stable)
 * Apache Airflow management API.
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: dev@airflow.apache.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import DagState from './DagState';

/**
 * The DAGRun model module.
 * @module model/DAGRun
 * @version 1.0.0
 */
class DAGRun {
    /**
     * Constructs a new <code>DAGRun</code>.
     * @alias module:model/DAGRun
     * @param dagId {String} 
     */
    constructor(dagId) { 
        
        DAGRun.initialize(this, dagId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, dagId) { 
        obj['dag_id'] = dagId;
    }

    /**
     * Constructs a <code>DAGRun</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DAGRun} obj Optional instance to populate.
     * @return {module:model/DAGRun} The populated <code>DAGRun</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DAGRun();

            if (data.hasOwnProperty('dag_run_id')) {
                obj['dag_run_id'] = ApiClient.convertToType(data['dag_run_id'], 'String');
            }
            if (data.hasOwnProperty('dag_id')) {
                obj['dag_id'] = ApiClient.convertToType(data['dag_id'], 'String');
            }
            if (data.hasOwnProperty('execution_date')) {
                obj['execution_date'] = ApiClient.convertToType(data['execution_date'], 'Date');
            }
            if (data.hasOwnProperty('start_date')) {
                obj['start_date'] = ApiClient.convertToType(data['start_date'], 'Date');
            }
            if (data.hasOwnProperty('end_date')) {
                obj['end_date'] = ApiClient.convertToType(data['end_date'], 'Date');
            }
            if (data.hasOwnProperty('state')) {
                obj['state'] = DagState.constructFromObject(data['state']);
            }
            if (data.hasOwnProperty('external_trigger')) {
                obj['external_trigger'] = ApiClient.convertToType(data['external_trigger'], 'Boolean');
            }
            if (data.hasOwnProperty('conf')) {
                obj['conf'] = ApiClient.convertToType(data['conf'], Object);
            }
        }
        return obj;
    }


}

/**
 * Run ID. The value of this field can be set only when creating the object. If you try to modify the field of an existing object, the request fails with an BAD_REQUEST error. If not provided, a value will be generated based on execution_date. If the specified dag_run_id is in use, the creation request fails with an ALREADY_EXISTS error. This together with DAG_ID are a unique key. 
 * @member {String} dag_run_id
 */
DAGRun.prototype['dag_run_id'] = undefined;

/**
 * @member {String} dag_id
 */
DAGRun.prototype['dag_id'] = undefined;

/**
 * The execution date. This is the time when the DAG run should be started according to the DAG definition. The value of this field can be set only when creating the object. If you try to modify the field of an existing object, the request fails with an BAD_REQUEST error. This together with DAG_ID are a unique key. 
 * @member {Date} execution_date
 */
DAGRun.prototype['execution_date'] = undefined;

/**
 * The start time. The time when DAG Run was actually created.. 
 * @member {Date} start_date
 */
DAGRun.prototype['start_date'] = undefined;

/**
 * @member {Date} end_date
 */
DAGRun.prototype['end_date'] = undefined;

/**
 * @member {module:model/DagState} state
 */
DAGRun.prototype['state'] = undefined;

/**
 * @member {Boolean} external_trigger
 * @default true
 */
DAGRun.prototype['external_trigger'] = true;

/**
 * JSON object describing additional configuration parameters. The value of this field can be set only when creating the object. If you try to modify the field of an existing object, the request fails with an BAD_REQUEST error. 
 * @member {Object} conf
 */
DAGRun.prototype['conf'] = undefined;






export default DAGRun;

