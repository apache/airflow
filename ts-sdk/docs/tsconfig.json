{
  "//": "Docs-only tsconfig for the TypeDoc analysis pass. It inherits the SDK's compiler settings verbatim and only narrows the program to the public sources: TypeDoc documents src/, and pulling tests/ in would slow the pass down without adding to the reference.",
  "extends": "../tsconfig.json",
  "include": ["../src/**/*.ts"]
}
