Fixed OTel metrics being exported as several conflicting cumulative streams. A process now builds a single metrics pipeline and reuses it, instead of leaving replaced ones exporting frozen totals alongside the live one — a scheduler previously ran three at once, one per copy of the metrics module plus one per re-initialisation. A forked child no longer exports the pipeline it inherited from its parent.
