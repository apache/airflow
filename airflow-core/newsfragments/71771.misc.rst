Deprecate ``DagFileProcessorManager.persist_parsing_result``; override ``persist_parsing_results`` instead, which is handed a group of the files that finished parsing together. Overriding ``handle_parsing_result`` is still supported, but stops parse results being persisted a group at a time.
