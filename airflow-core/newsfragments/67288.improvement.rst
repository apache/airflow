Improve performance of ``TaskGroup.topological_sort`` by projecting each child's per-task upstream IDs onto sibling-level integer indices once up front and sweeping with a ``bytearray``-backed emission flag, moving per-edge work out of the outer loop.
