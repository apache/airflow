``airflow db clean`` now purges the ``partitioned_asset_key_log`` table. It carries no foreign key, so nothing removed its rows when the partition Dag run they describe was cascade-deleted along with its ``dag_run``, and the table grew without bound. The test that is meant to catch a metadata table being left out of ``airflow db clean`` was also silently passing: it looked for the models package at a path that stopped existing when the sources moved under ``airflow-core``, so it examined no models at all and could not report anything missing.
