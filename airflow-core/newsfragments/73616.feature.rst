The *Tags* filter on the Dags list now has the "match anywhere" toggle introduced in #66015: enabling it switches the tag suggestion search from prefix matching (``tag_name_prefix_pattern``) to substring matching (``tag_name_pattern``), consistent with the other search inputs.
