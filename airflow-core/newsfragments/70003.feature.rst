Add ``pin_symlink_on_refresh`` option to ``LocalDagBundle``. When the bundle path is a symlink that an external synchronization process swaps atomically (e.g. the kubernetes git-sync sidecar), enabling this option resolves the symlink on each refresh and pins the resolved target until the next refresh, so a swap happening mid-way cannot mix files from two different targets within a single parse round or task run.
