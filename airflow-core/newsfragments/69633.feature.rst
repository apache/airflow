Embed a Dag run in an external trace by passing a W3C ``traceparent`` in the new ``airflow/dagrun_parent_trace_context`` run conf key. The whole run (the ``dag_run`` span and all task spans) is then traced inside that external trace instead of starting its own root trace; without the key, runs remain root traces as before.
