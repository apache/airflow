Fix ``airflow db clean --skip-archive`` still creating (and then dropping) the ``_airflow_deleted__*`` archive table for every batch. The command now deletes matching rows directly from the source table, so each batch does one write instead of a copy-and-drop pair -- avoiding the statement-timeout failures that could leave a partial archive table behind on deployments with tight timeouts.
