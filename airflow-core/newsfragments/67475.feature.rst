``Window`` subclasses accept a ``direction`` keyword — ``WindowDirection.FORWARD`` (default) fans out the period starting at the upstream key (forward in time); pass ``direction=WindowDirection.BACKWARD`` (e.g. ``WeekWindow(direction=WindowDirection.BACKWARD)``) to fan out the trailing period ending at the upstream key instead.
