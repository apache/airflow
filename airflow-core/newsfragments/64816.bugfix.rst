Fix ``TaskInstance.start_date`` not being restored for rescheduled tasks when the scheduler advances state to ``QUEUED``, which caused the ``dagrun.first_task_scheduling_delay`` metric to be inflated by reschedule wait time.
