The default task failure email subject no longer dumps every field of the task instance. ``{{ ti }}`` now renders the compact ``<TaskInstance: dag_id.task_id run_id [state] ti_id=...>`` form used elsewhere in Airflow, in email templates and anywhere else a Dag author interpolates it.
