The default task failure email no longer dumps every field of the task instance. ``{{ ti }}`` now renders the compact ``<TaskInstance: dag_id.task_id run_id [state] ti_id=...>`` form used elsewhere in Airflow, in email templates and anywhere else a Dag author interpolates it, and the default body of that email names the Dag, the task and the run.
