Scheduler heartbeat-timeout cleanup now sets ``task_callback_type`` on the ``TaskCallbackRequest``. Previously ``_purge_task_instances_without_heartbeats`` built the request without that field, defaulting it to ``None``; the Dag processor then dispatched ``on_failure_callback`` even when the task still had retries remaining, producing spurious failure alerts for tasks that ultimately succeeded on retry.
