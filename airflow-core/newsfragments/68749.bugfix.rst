New asset-triggered Dags no longer consume historical asset events recorded before the Dag started scheduling on those assets; the first run is now bounded at the moment the schedule reference was created instead of reprocessing the entire backlog (#39456).
